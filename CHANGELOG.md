# 更新日志

本文件记录 Journa 网页版的重要功能与体验更新。

## 2026-07-28

### 优化（移动端自然语言录入 · 自然性）

- 编辑器切换改为连续形变：舞台已展开时点另一个句子 token，不再硬关硬开，而是舞台滑向新词、clip-path 重锚、内容瞬时离场后淡入，读感像「编辑器在词与词之间移动」。
- 居中标题防截断：舞台标题由 `inline-flex` 改 `inline-block` 并加 `text-overflow: ellipsis`，打开态 `width:auto` + `max-width:calc(100% - 24px)`，长类别名 / 备注不再被固定锚点宽度裁切。
- 背景与舞台节奏对齐：聚焦背景 `opacity` 过渡 240ms → 360ms，与舞台生长（420ms）/ 缩回（360ms）主轴贴合，消除背景过早「到顶」的脱节感。
- 修复收回时高亮词与原句词同时隐藏形成空白：舞台标题现在沿原展开路径反向归位，并在玻璃框收拢结束后无缝交还给原 token。
- 修复收回交接末帧的字段跳动：关闭态复用原 token 的 flex 垂直居中与底边框占位，舞台字段和最终字段的文字基线完全重合。
- 恢复字段随卡片展开/收起平滑居中：舞台字段全程保持同一 flex 盒模型，移除开合边界处 `inline-block` / `inline-flex` 的离散切换。
- 优化收缩尾段：舞台淡出提前与最后 60ms 收缩重叠，原字段同步交叉淡入，清理时点由 480ms 收紧到 420ms，消除“停一下再消失”。
- 精修末尾圆角框：将毛玻璃外壳拆为独立合成层，在容器清理前完成 160ms 外壳溶解；字段与高光分别交接，避免 Safari 直接摘除 `clip-path + backdrop-filter` 图层造成硬消失。
- 微调自然语言卡片动效曲线：外壳采用轻快展开、柔和落定的非对称缓动；字段加入克制的中心过冲与归位回弹，收起和外壳溶解仍保持连续交接。

## 2026-07-18

### 优化

- 同步指示灯错误态细化：区分「云端不可用」（项目被暂停 / 网络异常）、「链接已失效」（对应云账本不存在或已删除）、「同步失败」（通用）三种原因，并在同步结束后沿用具体失败标签，便于快速定位问题。
- 主题预设与金额数字字重收敛：移除冗余 `--fw-regular`，全站金额 / 数字统一 `--fw-medium(500)`。
- Safari 健壮性提升：localStorage 写入与剪贴板复制失败均加兜底提示，避免私密模式 / 配额超限 / ITP 清空时事件链崩溃且无感知；类别新增入口重构为 chips 下方独立悬浮按钮，横滑橡皮筋桌面端改用原生 overscroll、触摸端保留阻尼回弹并加 `translateZ(0)` 保持合成层。
- 暗底英雄卡片 bloom 治理：暗色下改用派生的低 alpha `--bloom-*` 变量重做「吸收入账」动画，整体亮度压低，避免暗底炸开；不再引用 JS inline 的高亮变量以免盖掉暗色压低。
- 移动端面板切换入场阴影改为随 opacity 淡入，移除无法插值的 `mobile-panel-shadow-in` keyframes，避免动画末帧阴影「啪」地弹出（跨主题 / 跨卡片更稳）。
- 更新静态资源与 Service Worker 缓存版本（safari-fulledge）。

### 修复

- 修复英雄总支出卡片光晕被裁切：移除 `.metric.primary` 的 `overflow:hidden` 并内收氛围光晕位置，渐变与家庭色氛围光不再被边缘切掉。
- 修复首次加入云账本时本地空账本 `updatedAt=now` 误判比远端新、覆盖远端真实设置：新增 `lastSyncedAt`，仅当本地存在未同步设置改动（`updatedAt > lastSyncedAt`）时才保留本地设置。
- 新增墓碑垃圾回收：已同步到云端的删除条目真正移除，避免长期使用 localStorage 被撑爆；统计改为只计算未删除账单。

### 新增

- 新增「添加类别」入场动画，并补充设计预览页（英雄光晕 / 同步状态 / 主题预设 / 边框 / 深色层次）。

## 2026-07-17

### 新增

- 使用指南新增编辑者家庭归属步骤：云账本必须选择所属家庭，本地账本可跳过，并可在个人设置中随时调整。
- 账单创建与更新记录改为显示编辑者所属家庭，兼容旧姓名数据但不再收集或展示姓名。

### 优化

- 深色模式层次感提升：重定 `--shadow-*` 为真实黑色投影，静态面板（记账/统计/账单/平账等）叠加顶亮底暗玻璃边光，悬浮层玻璃提亮；卡片在暗底上重新浮起、不再发灰贴底。
- 更新静态资源与 Service Worker 缓存版本，确保新的深色层次样式及时生效。

### 修复

- 修复「复制邀请链接」始终指向硬编码的 GitHub Pages 地址：改为基于当前部署地址生成，部署到任意域名或本地 `localhost` 时邀请链接都能正确指向实际位置；`file:` 协议下返回空并提示先发布。

## 2026-07-15

### 优化

- 优化账本状态胶囊在移动端收起/展开的动画性能：胶囊外壳拆分为独立图层整体淡出，收放过程只保留位移与透明度动画，宽高、内边距等布局变化延迟到淡出后一次性完成，不再逐帧触发头部毛玻璃重绘，解决 iOS 上切换卡顿。
- 转账卡定稿为航线图样式：两端为主题色家庭名，中间一条随卡片宽度伸缩的虚线航线（付款家→收款家渐变色），箭头改为圆角 chevron 并配柔光，替换此前的尖角小三角。
- 修复移动端「数据」页滚到底部时，悬浮的圆形快速记账按钮压住平账建议卡片的问题：预留高度不再是脱节的固定像素，改为跟随按钮离底偏移与直径联动的变量，窄屏下也不会因为按钮尺寸不变而重新出现覆盖。
- 更新 Service Worker 缓存版本，确保发布后及时加载新界面。

## 2026-07-14（晚间）

### 优化

- 资金流向图改为纯视觉呈现：去掉飘带上的金额标签与节点旁的待转/待收小字，放大家庭名称，金额统一看下方转账卡，不再互相遮挡。
- 移除“旅程收尾”摘要卡：三个家庭的转账笔数几乎恒定，笔数保留在面板徽章与数据页入口中，“金额随账单实时重算”并入面板说明文字。
- 转账卡重排为 Z 字形路线：付款方在上、收款方错位在下，放大的淡色渐变箭头沉入背景沿对角指向收款方，去掉左侧圆角矩形节点，家庭名称与转账金额同步放大。
- 更新 Service Worker 缓存版本，确保发布后及时加载新界面。

## 2026-07-14

### 新增

- 新增金额小数显示开关，可在设置中选择金额保留两位小数或按整数显示；偏好保存在当前设备，且不影响精确到分的计算与导出。
- 新增平账资金流向图，用付款方、收款方、金额和动态光带直观展示每笔转账关系。
- 新增平账状态徽章与“已两清”完成状态，方便快速确认待结算笔数。

### 优化

- 重构平账入口：数据页改为简洁摘要，完整平账建议移入设置面板，点击入口后可直接定位查看。
- 重新设计转账明细卡片，强化付款方、收款方、转账顺序和金额层级，并优化数字对齐。
- 为资金流向、转账卡片和完成状态补充渐进式入场动效，同时遵循系统的“减少动态效果”偏好。
- 完善窄屏和深色模式下的平账布局、字号、颜色与对比度表现。
- 更新静态资源与 Service Worker 缓存版本，确保发布后及时加载新界面。
